import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  body: {
    margin: 15
  },
  profilePic: {
    alignItems: "center",
    gap: 5
  },
  name: {
    fontWeight: "700",
    fontSize: 18
  }
});
