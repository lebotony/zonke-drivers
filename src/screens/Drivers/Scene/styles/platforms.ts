import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  platformTags: {
    gap: 5,
    flexDirection: "row",
    flex: 1
  },
  platform: {
    paddingVertical: 4,
    paddingHorizontal: 5,
    justifyContent: "center",
    borderRadius: 10
  }
});
